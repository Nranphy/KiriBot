# KiriBot 项目背景

## 文档定位

本文记录当前已经明确的项目背景与开发边界，帮助开发者和 AI Agent 区分已确认事实与待定事项。

## 当前状态

KiriBot 正在当前仓库中开展设计与开发。

当前已实现 Manager 的配置加载、Loguru 日志、HTTP 启动、Playwright 无头浏览器检查、Web `/health` 接口及 Bot `/health` 管理命令。Controller 已装配 NoneBot2 的 OneBot v11 和 Satori Adapter；Gateway 已支持外部 OneBot v11 Universal 正向与反向 WebSocket、外部 Satori 正向连接、内部客户端事件转发，以及 OneBot Action 关联和 Satori HTTP API 代理。Manager 已支持通过配置启动、停止、重启和查询 Worker 子进程，并通过 HTTP API 与机器人管理命令提供实例管理操作；Worker 启动时由 Manager 注入 Gateway 地址、运行期 Token 和监听端口。NoneBot2 Worker 模板当前提供 `/echo` 命令。其他连接模式与其余能力仍处于设计阶段，本文及总体架构文档中的目标设计不表示已经实现。

## 技术栈

Manager 使用 Python，Worker 是语言不限的独立程序。Python 代码生成与维护遵循 [Python 代码风格](03.python-style.md)。

Manager 的 Controller 使用 NoneBot2 及现成的 OneBot、Satori Adapter 与 Gateway 交互，并使用 FastAPI 提供 WebUI 管理接口和 Worker 内部 HTTP 接口。Manager 和 Worker 都通过 Gateway 提供的标准内部 OneBot 或 Satori 地址接收事件，内部连接不感知外部连接方向。Worker 通过 HTTP 调用 Manager 公共能力。本仓库维护按机器人实现命名的 Worker 模板，当前仅规划 NoneBot2 模板，其命令能力接入 Alconna，并支持 NoneBot 插件市场；后续可增加 Koishi 等实现。Manager 管理命令也接入 Alconna，日志使用 Loguru，HTML 图片渲染选用 Playwright 与 Chromium。Gateway 已支持从 OneBot 11 和 Satori 消息事件异步记录 QQ 用户、平台身份和群聊，记录失败不阻断原始事件转发。权限服务已支持配置文件中的全局超级管理员和黑名单，以及数据库中的多权限、群聊范围、过期和优先级计算；管理入口和具体功能授权尚未接入。数据库通过 SQLAlchemy 异步连接 URL 配置，当前仅支持使用 aiosqlite 驱动的 SQLite；当前表结构在应用启动时幂等初始化，后续结构升级仍规划使用 Alembic 管理迁移。当前工程使用 Python 3.14 与 uv，Ruff 用于代码检查和格式化，pytest 与 pytest-asyncio 用于测试，Pyright 用于类型检查；框架及业务依赖的具体版本以实际项目配置和锁文件为准。

## 产品目标

KiriBot 是一个支持集中管理和持续迭代的机器人系统。长期运行的 Manager 管理机器人实例、用户、权限、数据和公共工具；独立进程 Worker 执行插件功能；Gateway 对接外部组件并路由机器人通信。

KiriBot 的产品定位是支持跨机器人框架、跨聊天平台的单一机器人应用。Manager、Gateway、各个外部连接和不同实现的 Worker 共同组成一个完整机器人；功能和用户体验不应因外部连接、聊天平台或底层 Worker 不同而被设计成多个彼此独立的机器人。使用不同配置和路由规则在技术上可能形成类似多个机器人的表现，但这不属于项目的设计目标。

管理操作同时面向 WebUI 和具有管理员权限的机器人指令。机器人功能以插件为开发单位，按各实现接入命令解析，通过 Manager HTTP 接口使用权限和数据库等公共能力，支持 AI 参与开发与调试。

机器人代码迭代遵循：触发 → 开发 → 调试 → 审批 → 部署。

## 核心概念

| 概念 | 含义 |
| --- | --- |
| Manager | 长期运行的主程序，集中提供管理服务与公共能力 |
| Controller | Manager 内的管控层，包含 FastAPI 管理接口和 NoneBot2 管理指令入口 |
| Worker / 机器人实例 | 由 Manager 配置管理的独立机器人程序，通过 OneBot 或 Satori 连接 Gateway，负责执行插件 |
| Gateway | 对接外部组件，向 Controller 和 Worker 提供 OneBot、Satori 连接与协议转发 |
| 用户 | 从外部事件维护的平台用户身份，是权限判断的基础 |
| 插件 | Worker 功能的开发、启用和迭代单位 |
| 工具组 | Manager 通过内部 HTTP 接口提供数据库、渲染、网络请求、AI 和权限判断等公共能力 |

## MVP 范围

MVP 只提供一个 Worker 实例，保留清晰的实例管理和路由边界，不提前实现多实例调度。其余能力是目标设计的一部分，首版的具体交付顺序与范围仍需确定。

## 待确定事项

机器人通信使用 OneBot 和 Satori。具体协议版本、传输模式、共享账号的事件分发与请求关联、内部 HTTP 接口的具体契约与认证方式、权限管理入口和功能授权接入、插件契约、调试环境、部署与回滚方式，以及用户与权限数据之外的数据库接口仍待确定。后续根据需求和实际实现逐步记录，不为了补齐文档而编造这些内容。

项目目录按 [目录结构](05.directory-structure.md) 组织。形成实际模块后，再补充模块内部职责和必要的架构文档。

## 文档入口

- [AGENTS.md](../AGENTS.md)：AI Agent 的阅读顺序、工作流程和约束
- [代码与文档规范](02.conventions.md)：模型生成代码、注释、文档和交付说明的统一风格
- [Python 代码风格](03.python-style.md)：Python 类型、异步处理、数据模型和职责边界约定
- [总体架构](04.architecture.md)：Manager、Worker、Gateway 的职责与主要流程
- [目录结构](05.directory-structure.md)：源码、管理界面、部署配置与测试的放置约定
