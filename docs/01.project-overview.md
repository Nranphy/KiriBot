# KiriBot 项目背景

## 文档定位

本文记录当前已经明确的项目背景与开发边界，帮助开发者和 AI Agent 区分已确认事实与待定事项。

## 当前状态

KiriBot 正在当前仓库中开展设计与开发。

当前仓库处于设计阶段。本文及总体架构文档描述目标设计，不表示相关能力已经实现。

## 技术栈

本项目使用 Python。代码生成与维护遵循 [Python 代码风格](03.python-style.md)。

Worker 的插件运行计划基于 NoneBot 生态，命令能力接入 Alconna，并支持 NoneBot 插件市场。当前工程使用 Python 3.14 与 uv，Ruff 用于代码检查与格式化。Manager 的 Web 框架及业务依赖尚待确定，具体版本以项目配置和锁文件为准。

## 产品目标

KiriBot 是一个支持集中管理和持续迭代的机器人系统。长期运行的 Manager 管理机器人实例、用户、权限、数据和公共工具；独立进程 Worker 执行插件功能；Gateway 对接外部组件并路由机器人通信。

管理操作同时面向 WebUI 和具有管理员权限的机器人指令。机器人功能以插件为开发单位，统一接入命令解析、权限和数据库能力，支持 AI 参与开发与调试。

机器人代码迭代遵循：触发 → 开发 → 调试 → 审批 → 部署。

## 核心概念

| 概念 | 含义 |
| --- | --- |
| Manager | 长期运行的主程序，集中提供管理服务与公共能力 |
| Worker / 机器人实例 | 独立运行并连接 Gateway 的机器人进程，负责执行插件 |
| Gateway | 对接 OneBot、Satori 外部组件及 Worker 的通信入口 |
| 用户 | 从外部事件维护的平台用户身份，是权限判断的基础 |
| 插件 | Worker 功能的开发、启用和迭代单位 |
| 工具组 | 向插件提供数据库、渲染、网络请求、AI 和鉴权 Depend 等能力的统一入口 |

## MVP 范围

MVP 只提供一个 Worker 实例，保留清晰的实例管理和路由边界，不提前实现多实例调度。其余能力是目标设计的一部分，首版的具体交付顺序与范围仍需确定。

## 待确定事项

进程通信协议、数据库选型、权限模型、插件契约、调试环境、部署与回滚方式，以及测试工具尚未确定。后续根据需求和实际实现逐步记录，不为了补齐文档而编造这些内容。

本文不预设项目目录结构。形成实际模块后，再为相关模块补充职责和必要的架构文档。

## 文档入口

- [AGENTS.md](../AGENTS.md)：AI Agent 的阅读顺序、工作流程和约束
- [代码与文档规范](02.conventions.md)：模型生成代码、注释、文档和交付说明的统一风格
- [Python 代码风格](03.python-style.md)：Python 类型、异步处理、数据模型和职责边界约定
- [总体架构](04.architecture.md)：Manager、Worker、Gateway 的职责与主要流程
