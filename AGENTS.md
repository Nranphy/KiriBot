# AGENTS.md

本文件是 AI Agent 在本仓库工作的入口。
开始修改前，先阅读 [项目背景](docs/01.project-overview.md) 和 [代码与文档规范](docs/02.conventions.md)；编写或修改 Python 代码前，还应阅读 [Python 代码风格](docs/03.python-style.md)。

## 工作流程

1. 检查工作区状态，了解已有修改，不覆盖或回退用户的工作。
2. 根据修改范围阅读相关 `README.md`、`docs/` 和更深层的 `AGENTS.md`。同时检查当前目录及其父级目录的相关文档。
   涉及管理服务、机器人通信或插件功能时，阅读 [总体架构](docs/04.architecture.md)，区分目标设计与已实现能力。
3. 参考同一目录、同一模块和相邻文件的实现，确认现有命名、错误处理、注释、测试与工具约定。
4. 完成当前需求所需的修改，优先复用已有实现，避免无关重构、依赖调整和提前设计。
5. 根据修改影响进行验证；行为、接口或约定变化时，同步更新相关文档。
6. 用中文说明最终结果、验证情况，以及实际存在的限制或待解决问题。

## 约束与优先级

- 用户的明确要求优先于仓库文档。若与长期约定不同，说明差异；不要将一次需求自动扩展为长期规范变更。
- 更深层的 `AGENTS.md` 在其目录范围内补充或覆盖本文件；普通项目文档冲突时，优先采用最接近修改位置的约定。
- 文档与实际实现不一致时，检查相关代码并说明差异，不凭空补全未确认的产品需求或架构。
- 本项目使用 Python 技术栈，Worker 计划基于 NoneBot 插件生态。具体版本、Manager 框架、目录结构和运行方式根据本项目需求确定。
- 提交、推送、发布和历史改写按用户授权范围执行，不把代码修改请求自动理解为这些操作的授权。

详细的生成风格、注释、接口、文档、验证和 Git 规范见 [代码与文档规范](docs/02.conventions.md)。
